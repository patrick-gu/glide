func identity<T>(t T) T {
    t
}

func void() {}

func main() {
    identity(void())
    identity(123)
    println(identity(""))
    println(identity(identity("zzz")))
    identity(void)
    identity(main)
    identity(identity)(println)("t")
}
